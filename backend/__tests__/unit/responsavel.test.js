const Sequelize = require("sequelize");
const Responsavel = require("../../src/models/responsavel");
const sequelize = new Sequelize("ciranda", "root", "");

describe("Responsavel", () => {
  it("should create a reponsible for the child", async () => {});
});
